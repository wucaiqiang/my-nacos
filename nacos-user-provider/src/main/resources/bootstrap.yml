spring:
  application:
    name: user-provider
  cloud:
    nacos:
      config:
        server-addr: config.server.com:8848
        file-extension: properties
        name: ${spring.application.name}
        username: nacos
        password: Qq5201314.?
      discovery:
        server-addr: register.server.com:8848
        service: ${spring.application.name}
        username: nacos
        password: Qq5201314.?



